# [功能名] Plan

<!--
第一期写全量架构；第二期之后只写增量。
让 agent 读全量架构，它会重新设计已经存在的东西。
本文档与语言相关，按项目实际技术栈写。
-->

## 改动范围

<!-- 「不改动」那一行是 ROI 最高的一行，专门写给 agent。不写它，agent 会"顺手改进"相邻模块 -->

| 文件 | 操作 | 说明 |
|---|---|---|
| `path/to/a` | 新建 / 重构 / 小改 | |
| `path/to/b` | | |

不改动：`path/c/*`、`path/d/*`

## 核心结构变更

<!--
现状 → 目标，用伪代码或文字。
伪代码控制在 20 行内。超过就是在写实现，退回去。
-->

现状：

```
```

目标：

```
```

## 核心数据结构与接口

<!-- 只列签名和字段，不列实现。第二期之后只列 diff -->

```
```

## 模块设计

<!-- 每个模块：职责 / 对外接口 / 依赖。已存在且不改的模块不用写 -->

### [模块名]

**职责：**

**对外接口：**

**依赖：**

## 模块交互

<!-- 调用链、数据流。哪个模块调哪个，什么顺序 -->

```
```

## 关键决策

<!-- 同时写选择和理由。理由是给三个月后的你看的 -->

| 决策点 | 选择 | 理由 |
|---|---|---|
| | | |

<!--
自检：
- spec 的每条功能需求是否都在架构中有归属？列出缺口
- 光看接口描述，能不能独立实现每个模块？
- 依赖是否无环？
- 有没有技术决策和 spec 的假设冲突？
-->
