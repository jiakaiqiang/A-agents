# [功能名] Checklist

<!--
必须在编码前写完。它不是事后验收表，是编码前把"做对了"的定义钉死。
缺了它就是没靶子开枪，最后只能靠"看起来差不多"收工。

两条写法：
  可观测 —— 每项都是「做 X，看到 Y」或「运行 X，期望 Y」
  与实现解耦 —— 重命名文件或移动函数后不该失败；会失败的那条在测实现，重写它
-->

> 每一项通过运行代码或观察行为来验证，验证方式写在括号里。

## 行为

<!-- spec 的验收标准逐条落地，每条记录真实命令输出或屏幕内容 -->

- [ ] AC1 — （验证：）
- [ ] AC2 — （验证：）

## 不变式

<!--
结构性约束，每条退出路径各验一次。
例：每个请求都有配对的响应记录 / 任何退出路径下资源都被释放 / 历史结构合法
-->

- [ ] 正常路径：
- [ ] 失败路径：
- [ ] 中断路径：

## 回归

<!-- 老功能没被弄坏。第二期之后必须有这一块 -->

- [ ] 上一期的端到端场景重跑通过
- [ ] 类型检查 / 编译无错误（验证：`<命令>` 退出码 0）
- [ ] 全部测试通过，**无跳过用例**（验证：`<命令>` → `N pass / 0 fail`）
- [ ] lint 通过（如有配置）

## 端到端

<!-- 至少一个走完整流程的场景。含一个失败场景和一个中断场景更好 -->

- [ ] 场景 A（正常）：<完整用户操作> → <可观测结果>
- [ ] 场景 B（失败）：<触发失败的操作> → <预期表现，且程序不崩>
- [ ] 场景 C（中断）：<中途中断> → <界面响应，可继续>

---

<!--
验收时填写下面这段，先有证据再下结论：先跑命令，看输出，再报状态。
报告实际结果，不是预期结果。
-->

## 验收报告

### 通过（N/M）

- [x] AC1 — 证据：

### 未通过

- [ ] ACn — 预期：X；实际：Y。原因：<定位>。修复：<方案>

### 未验证

- [ ] <项> — 原因：<环境限制等>。等价路径已由 <某测试> 覆盖，建议 <手动补法>
