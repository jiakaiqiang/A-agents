# [功能名] Tasks

<!--
让 agent 按你想法干活的核心。每个任务三件套缺一不可：
  锚点 — 具体文件路径 + 函数名（缺了 agent 自己找地方改）
  验证 — 可粘贴的命令 + 预期输出（缺了"应该没问题"会当成结论）
  禁止 — 具体文件名或函数名（缺了会顺手重构邻近模块）

不要贴真代码。判据：读过 plan.md 的人删掉某段仍能写出等价代码 → 该段删掉。
粒度 2-5 分钟，超过就拆。
-->

## 全局约束

<!-- 每个任务都适用。最后一条是控制偏离最有效的单条规则，不要删 -->

- 不新增第三方依赖
- 注释用中文，风格匹配现有文件
- 只改「改动范围」表里的文件
- 每个任务完成后跑该任务的验证命令，贴出真实输出，然后停下等我确认，不要连着做下一个

## 文件清单

| 操作 | 文件 | 职责 |
|---|---|---|
| | | |

---

## T1: [任务名]

<!-- 第一刀做纯机械、不改行为的变更（改签名、加字段），先拿到一次干净的绿色验证作基线 -->

**文件：** `path/to/file`（函数 / 类名）
**依赖：** 无
**步骤：**
1.
2.

**验证：** `<可粘贴的命令>` → `<预期输出>`
**禁止：** <具体文件名或函数名>

---

## T2: [任务名]

**文件：** `path/to/file`
**依赖：** T1
**步骤：**
1.
2.

**验证：** `<命令>` → `<预期输出>`

<!--
预期会失败的地方必须写明，例：
  旧的 X / Y 两个用例会失败，这是预期的（行为已变更），
  本任务只报告失败清单，不要改测试。
不写这句，agent 会顺手把测试改绿，你就永久失去了"行为确实变了"的信号。
-->

**禁止：**

---

## T3: [任务名]

**文件：**
**依赖：**
**步骤：**
1.

**验证：**
**禁止：** 不放宽既有断言来让测试变绿

---

## 执行顺序

```
T1 → T2 → T3
```

<!--
自检：
- plan 的每个组件是否至少有一个任务？
- 是否存在合法执行顺序，无循环依赖？
- 有没有哪个任务缺「验证」或「禁止」？
- 有没有哪段是代码转录，可以删掉？
-->
